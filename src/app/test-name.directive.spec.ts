import { TestNameDirective } from './test-name.directive';

describe('TestNameDirective', () => {
  it('should create an instance', () => {
    const directive = new TestNameDirective();
    expect(directive).toBeTruthy();
  });
});
